  //como usar interface para o user clicar com o mouse.
  //não preciso passar informação então é void
  // ./escola abre o exe
  //gcc nomedoprogama.c -o( de open) nomequequero.exe
  //quando uma função retorna algo eu tenho que explicar esse algo
  //sempre que mudar a função tem que mudar o protótipo
  //Faça um programa que leia 2 números e some os resultados. No main vai ler os dois   Chamando função.
  //https://animesgames.net/ assistir animes online
//o main que tem o controle dos outros módulos do programa. Ele tem de chamar as variáveis(projeto escola);
//eu coloco o valor negativo no define pra evitar os valores nos ifs
//Quando vc passa um vetor dentro da função é como se fosse analogo a referência, como se fosse um ponteiro; Isso quando mexer o vetor com parâmetro;
//Toda vez que passar variáveis por parâmetro, todas são cópias e os valores delas ficam na função assim que as mesmas acabarem;
//getchar pega o digito ramdom do buffer;
